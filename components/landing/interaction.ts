// Shared interaction feel for the landing hero's interactive elements: one
// corner radius, one spring easing, and a subtle press-scale — applied to every
// button / control so they read as one coherent, tactile shape language.
// (Emil Kowalski style: motion only on interaction, never ambient.)
export const PRESS =
  "rounded-xl transition-all duration-150 ease-[cubic-bezier(0.32,0.72,0,1)] active:scale-[0.98]";
